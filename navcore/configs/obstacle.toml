#==========================================================================
# Obstacle Configuration file
#==========================================================================

[table]
num_tables = 5
shape = "circular"
radius = 0.5
randomize_radius = true

[boundary]
gate = { enabled = true, num_gates = 2 }
